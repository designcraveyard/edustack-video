name: eduplugin

services:
  app:
    build:
      context: https://github.com/designcraveyard/edustack-video.git#main:vps/app
    container_name: eduplugin-app
    restart: unless-stopped
    environment:
      EDUPLUGIN_DATA_DIR: /var/lib/eduplugin
      EDUPLUGIN_BIND: 0.0.0.0:8000
    volumes:
      - eduplugin-data:/var/lib/eduplugin
    expose:
      - "8000"

  caddy:
    build:
      context: https://github.com/designcraveyard/edustack-video.git#main:vps/caddy
    container_name: eduplugin-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - app

volumes:
  eduplugin-data:
  caddy-data:
  caddy-config:
